<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stepin2it">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permission to make a phone call -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- External storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Stepin2ItApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.SplashScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity
            android:name=".ui.activity.DashBoardActivity"
            android:theme="@style/DashBoardTheme" />
        <activity android:name=".ui.activity.ProductImageActivity" />
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:label="@string/settings_label"
            android:parentActivityName=".ui.activity.DashBoardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.stepin2it.ui.activity.DashBoardActivity" />
        </activity>
        <activity
            android:name=".ui.activity.ProductDetailsActivity"
            android:parentActivityName=".ui.activity.DashBoardActivity"
            android:theme="@style/DetailActivityTheme">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.stepin2it.ui.activity.DashBoardActivity" />
        </activity>

        <!--API key for google maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCzyteUEKvdEK-GnOReHqOb58DqAmb1PY8" />

        <service android:name=".service.MusicPlayerService" />
        <service android:name=".service.MyFireBaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".service.MyFireBaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>